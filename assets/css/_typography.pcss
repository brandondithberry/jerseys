@layer base {
  article {
    max-width: 80ch;
    @apply mx-auto;
  }

  h1,
  h2,
  h3,
  h4,
  h5,
  h6,
  p {
    color: var(--text);
    @apply transition-colors duration-200 ease-in-out;
    & + h1,
    & + h2,
    & + h3,
    & + h4,
    & + h5,
    & + h6,
    & + p {
      @apply mt-4;
    }
  }

  h1 {
    @apply font-title
      font-bold
      text-2xl
      uppercase
      tracking-widest;
  }

  h2 {
    @apply font-title
      font-semibold
      text-xl
      leading-normal
      tracking-normal;
  }

  h3 {
    @apply font-title
      font-bold
      text-lg
      leading-normal;
  }

  h4 {
    @apply font-title
      font-normal
      leading-relaxed
      tracking-wide;
  }

  h5 {
    @apply text-lg
      leading-tight
      tracking-wide;
  }

  h6 {
    @apply font-light
      text-lg
      leading-tight
      tracking-wide
      uppercase;
  }

  p {
    color: var(--text);
    @apply font-normal leading-normal transition-colors duration-200 ease-in-out;

    & + h1,
    & + h2,
    & + h3,
    & + h4,
    & + h5,
    & + h6,
    & + p {
      @apply mt-4;
    }

    & a[href] {
      color: var(--text-gray);

      @apply underline;
    }
  }

  a {
    color: var(--text-gray);
    @apply transition-colors duration-200 ease-in-out;
  }

  .nuxt-content {
    max-width: 80ch;
    @apply my-8;

    & h1,
    & h2,
    & h3,
    & h4,
    & h5,
    & h6 {
      & > a {
        &::before {
          content: '#';
          color: var(--text-gray);
          @apply -ml-6 opacity-0 transition-opacity duration-100 ease-in-out;
        }
      }
      &:active,
      &:hover,
      &:focus {
        & > a::before {
          @apply opacity-100;
        }
      }
    }
    & p {
      & + h1,
      & + h2,
      & + h3,
      & + h4,
      & + h5,
      & + h6 {
        @apply mt-8;
      }
    }
    & h3 {
      @apply text-lg
        font-bold;
    }
    & img {
      @apply mx-auto my-4 rounded;
    }
    /*! purgecss start ignore */
    & ol > li,
    & ul > li {
      color: var(--text);
      @apply font-normal leading-normal transition-colors duration-200 ease-in-out;
    }
    & > ol {
      @apply list-decimal grid gap-y-2 my-4 ml-4;
      & ol {
        @apply list-decimal grid gap-y-1 ml-8 mt-2;
      }
    }
    & > ul {
      @apply list-disc grid gap-y-2 my-4 ml-4;
      & ul {
        @apply list-disc grid gap-y-1 ml-8 mt-2;
      }
    }
    & code,
    & kbd,
    & samp {
      color: var(--text-gray);
      background-color: var(--);
      @apply p-1 rounded transition-colors duration-200 ease-in-out;
    }

    & .nuxt-content-highlight {
      @apply relative my-4;
      & > .filename {
        color: var(--text-gray);
        text-shadow: 0 1px rgba(255, 255, 255, 0.5);
        @apply absolute right-0 top-0 z-10 font-mono text-sm leading-5 mr-3 mt-2;
      }
      & pre[class*='language-'] {
        background-color: var(--);
        text-shadow: 0 1px rgba(255, 255, 255, 0.5);
        @apply p-4 rounded transition-colors duration-200 ease-in-out;
      }
      & .token.operator {
        background: none;
      }
    }
  }
  /*! purgecss end ignore */

  @screen sm {
    .nuxt-content {
      & ul {
        @apply ml-0;
      }
    }
  }

  @screen md {
    h1 {
      @apply text-4xl;
    }

    h2 {
      @apply text-3xl;
    }

    h3 {
      @apply text-2xl;
    }

    h4 {
      @apply text-xl;
    }

    h5,
    h6 {
      @apply text-lg;
    }
  }
}
